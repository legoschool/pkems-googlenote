# 🚚 배포 가이드 — 나눠 쓰는 방법

> 이 앱은 **서버가 없습니다.** 각 사용자의 브라우저가 **자기 구글 계정**으로
> **자기 드라이브**에 직접 저장합니다. 배포하는 사람은 저장 공간도, 서버 비용도,
> 남의 데이터 관리 책임도 지지 않습니다.
>
> 다만 구글 권한 규칙 때문에 **"링크만 뿌리면 아무나"는 안 됩니다.** 아래 두 갈래 중 하나를 고르세요.

---

## 먼저: 왜 그냥 링크만 뿌리면 안 되나

이 앱은 사용자가 **드라이브 폴더를 직접 지정**합니다. 그러려면 `.../auth/drive` 권한이 필요한데,
구글은 이것을 **제한된 범위(restricted scope)** 로 분류합니다.

- 심사를 안 받은 앱은 **테스트 사용자로 등록된 사람만** 로그인됩니다
- 등록 안 된 사람은 경고가 아니라 **로그인 자체가 거부**됩니다 (`403 access_denied`)
- 한 앱(=한 클라이언트 ID)당 테스트 사용자 **최대 100명**

그래서 방법은 둘입니다.

| | 🅐 내 배포본에 초대하기 | 🅑 사본을 떠서 각자 배포 |
|---|---|---|
| **받는 사람이 할 일** | 링크 열고 로그인 (설정 0) | GitHub + 구글 콘솔 설정 (약 10분) |
| **내가 할 일** | 이메일을 콘솔에 등록 | 파일만 넘겨주면 끝 |
| **인원** | 최대 100명 | 사본마다 100명씩 (사실상 무제한) |
| **관리 책임** | 내가 계속 등록해 줘야 함 | 각자 알아서 |
| **어울리는 경우** | 우리 반, 우리 학교, 연수 한 기수 | 연수 강사가 참가자에게 "각자 만들어 가세요" |

> 🅐 로 시작해서, 100명이 차거나 관리가 버거워지면 🅑 로 넘기는 게 보통 맞습니다.
> 두 방식을 섞어도 됩니다.

---

# 🅐 내 배포본에 초대하기

**받는 사람은 아무 설정도 하지 않습니다.** 링크 열고 로그인만 하면 됩니다.

## A-1. 내가 하는 일 (사람 추가할 때마다)

1. [Google Cloud Console](https://console.cloud.google.com/) → 내 프로젝트
2. **Google 인증 플랫폼 → 대상**
3. **테스트 사용자 → + 사용자 추가**
4. 이메일을 넣고 저장 (여러 개 한꺼번에 가능)

바로 적용됩니다. 상대는 다시 로그인만 하면 됩니다.

> ⚠️ **「앱 게시」(프로덕션 전환) 버튼을 누르지 마세요.**
> 테스트 상태로 두는 게 맞습니다. 프로덕션으로 바꾸면 구글 심사 대상이 되고,
> 심사 없이는 오히려 더 불편해집니다 (100명 한도가 소진식으로 바뀌어 되돌리기 어려움).

## A-2. 받는 사람에게 보낼 안내

[사용안내.md](사용안내.md) 에 **그대로 복사해서 보낼 수 있는 안내문**이 있습니다.
카톡·메일에 붙여넣기만 하면 됩니다.

## A-3. 자주 나오는 질문

**Q. 내 드라이브에 남의 기록이 쌓이나요?**
아니요. 각자 **자기 계정, 자기 폴더**에 저장됩니다. 제 용량과 무관합니다.

**Q. 내가 남의 기록을 볼 수 있나요?**
없습니다. 서버가 없어서 데이터가 저를 거치지 않습니다.
브라우저 ↔ 구글 API 사이에서만 오갑니다.

**Q. 100명은 어떻게 세나요?**
테스트 사용자 **목록에 등록된 이메일 수**입니다. 실제 사용 여부와 무관합니다.
안 쓰는 사람을 지우면 자리가 납니다.

**Q. 한 번 로그인하면 계속 되나요?**
네. 이 앱은 갱신 토큰을 쓰지 않아 "테스트 앱 7일 만료" 규칙에 걸리지 않습니다.
브라우저에서 구글 로그아웃을 하거나
[계정 권한](https://myaccount.google.com/permissions)에서 연결을 끊으면 다시 로그인하면 됩니다.

---

# 🅑 사본을 떠서 각자 배포

받는 사람이 **자기 앱**을 갖는 방식입니다. 그 사람도 자기 100명을 새로 가집니다.
아래는 **사본을 받아가는 사람**이 하는 일입니다. (약 10분)

## B-1. 코드 사본 뜨기

- **GitHub 를 쓴다면:** 원본 저장소에서 **Fork** → 내 저장소 → **Settings → Pages →
  Build and deployment → Source** 를 **GitHub Actions** 로 → `main` 에 푸시하면 자동 배포
- **GitHub 가 없다면:** 폴더째 받아서 정적 호스팅 아무 데나 올려도 됩니다
  (Netlify 드래그&드롭, 학교 웹서버 등). **`https://` 로 열려야** 구글 로그인과 화면 캡처가 됩니다.

배포 주소가 나오면 **주소의 앞부분(원본, origin)** 만 적어 둡니다.

```
배포 주소   https://내계정.github.io/저장소이름/
쓸 값(원본) https://내계정.github.io
```

> 앱을 열고 **설정 → 고급** 에 가면 지금 주소의 원본이 그대로 표시됩니다. 복사해서 쓰면 확실합니다.

## B-2. 구글 클라우드 설정

[Google Cloud Console](https://console.cloud.google.com/)

| 순서 | 위치 | 할 일 |
|---|---|---|
| 1 | 왼쪽 위 프로젝트 선택 | **새 프로젝트** (이름 아무거나) |
| 2 | API 및 서비스 → **라이브러리** | `Google Drive API` → **사용 설정** |
| 3 | Google 인증 플랫폼 → **브랜딩** | 앱 이름(사용자에게 보임), 지원 이메일 |
| 4 | Google 인증 플랫폼 → **대상** | 사용자 유형 **외부**, 게시 상태는 **테스트** 유지, **테스트 사용자**에 본인 포함 등록 |
| 5 | Google 인증 플랫폼 → **데이터 액세스** | 범위 3개 확인 (아래) |
| 6 | Google 인증 플랫폼 → **클라이언트** | 웹 애플리케이션 클라이언트 생성 + 승인된 원본 등록 |

**5번 범위** — 이 3개가 있어야 합니다.

| 범위 | 용도 |
|---|---|
| `openid` | 로그인 |
| `.../auth/userinfo.email` | 저장 계정 화면 표시 |
| `.../auth/drive` | **지정한 폴더에 파일 만들기·읽기** |

`drive` 는 목록에서 잘 안 보입니다. **범위 추가 또는 삭제 → 아래 입력칸**에 직접 붙여넣고
**테이블에 추가** 하세요.

```
https://www.googleapis.com/auth/drive
```

**6번 승인된 자바스크립트 원본** — B-1에서 적어 둔 **원본 주소**를 넣습니다.

```
https://내계정.github.io
```

> 경로(`/저장소이름/`)는 **빼야** 합니다. 여기가 가장 많이 틀리는 곳입니다.

만들고 나오는 **클라이언트 ID**(`...apps.googleusercontent.com`)를 복사합니다.
같이 나오는 **클라이언트 보안 비밀번호는 쓰지 않습니다.** 저장소에 넣지 마세요.
(클라이언트 ID는 공개용 식별자라 코드에 있어도 안전합니다.)

## B-3. 코드에 심기 ⚠️ 이걸 안 하면 로그인이 안 됩니다

`index.html` 위쪽에서 이 줄을 찾습니다.

```js
var OWNER_CLIENT_ID = "255330405723-v0hdhjapm5aph1ht4eme11cmimn3o6kg.apps.googleusercontent.com";
```

따옴표 안을 **방금 발급받은 내 클라이언트 ID** 로 바꾸고 저장 → 푸시.

> 원본에 적힌 ID를 그대로 두면 안 됩니다. 그 ID는 **원본 주소에서만** 동작하도록
> 묶여 있어서, 내 주소에서는 로그인 창이 떴다가 바로 닫힙니다.
>
> 코드를 못 고치는 상황이면 앱의 **설정 → 고급 → 구글 OAuth 클라이언트 ID** 에 붙여넣어도
> 되지만, 그건 그 브라우저에만 저장됩니다. 남에게 나눠 줄 거면 반드시 코드에 심으세요.

## B-4. 확인

1. 배포 주소를 엽니다
2. **구글 로그인** → "확인되지 않은 앱" 경고 → **고급 → (앱 이름)(으)로 이동**
3. 드라이브 권한 **허용**
4. **설정 → 저장 위치** 에 폴더 링크를 붙여넣고 **이 폴더로 연결**
5. 아무거나 써서 저장 → 드라이브 폴더에 `.md` 가 생기면 성공

---

## 막혔을 때

| 증상 | 원인과 해결 |
|---|---|
| 로그인 창이 떴다가 바로 닫힘 | 승인된 자바스크립트 원본이 안 맞습니다. 경로 빼고 `https://계정.github.io` 만. 사본이면 **B-3(클라이언트 ID 교체)** 을 빠뜨렸을 가능성이 큽니다 |
| `403 access_denied`, "앱이 차단됨" | 테스트 사용자에 그 이메일이 없습니다 |
| "확인되지 않은 앱" 경고 | 정상입니다. **고급 → 이동** 으로 통과 |
| 로그인은 되는데 폴더 확인 실패 | `.../auth/drive` 범위가 빠졌습니다. 추가 후 다시 로그인해 권한 재동의 |
| "폴더가 아닙니다" | 파일 링크를 넣었습니다. 폴더를 열었을 때의 주소여야 합니다 |
| 화면 캡처 버튼이 안 먹음 | `https://` 또는 `localhost` 에서만 됩니다. `file://` 로 연 파일은 브라우저가 막습니다 |
| 휴대폰에 화면 캡처가 없음 | 정상입니다. 스크린샷을 찍고 **갤러리에서 고르기** 로 넣으면 PC와 같은 결과 |

---

## 나중에 "정말 아무나" 쓰게 하려면

`.../auth/drive` 대신 **`drive.file` (앱이 만든 파일만)** 로 낮추면
**심사도 인원 제한도 없어집니다.** 대신 폴더를 링크로 지정하는 대신
**구글 폴더 선택창(Picker)에서 클릭**해 고르게 됩니다 — 사용자 입장에선 오히려 더 쉽습니다.

바꾸려면 구글 콘솔에서 **Picker API 사용 설정 + API 키 1개 발급**이 추가로 필요합니다.
지금 방식(🅐/🅑)으로 부족해지면 그때 전환하세요.
